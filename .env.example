# Flask Configuration
# Generate a secure secret key with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=generate-a-random-secret-key-here
FLASK_ENV=development

# Database (use absolute path or relative)
DATABASE_PATH=GPTLeague.db

# Session Security (set to True in production with HTTPS)
SESSION_COOKIE_SECURE=False
